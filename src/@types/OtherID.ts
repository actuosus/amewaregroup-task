export interface OtherID {
    /**
     * The id of the 'other id'.
     */
  id: string;
  /**
   * The type of the other id.
   */
  type: string;
  /**
   * The issuer of the other id.
   */
  issuer: string;
  /**
   * The state of the other id.
   */
  state: string;
}
